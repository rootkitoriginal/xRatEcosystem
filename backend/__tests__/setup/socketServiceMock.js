/**
 * @deprecated Import from `tests/setup/socketServiceMock` instead.
 * This file remains for backward compatibility with legacy tests.
 */

const { createSocketServiceMock } = require('../../tests/setup/socketServiceMock');

module.exports = {
  createSocketServiceMock,
};
